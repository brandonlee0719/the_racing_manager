export const whatsapp = ({url}) => {
  return `Visit ${url} to see my horse!`
}

export const facebook = ({url}) => {
  return `Visit ${url} to see my horse!`
}

export const twitter = ({url}) => {
  return `Visit ${url} to see my horse!`
}

export const linkedin = ({url}) => {
  return `Visit ${url} to see my horse!`
}

export const email = ({url}) => {
  return {
    subject: `Please visit TRM to see my horse`,
    body: `Visit ${url} to see my horse!`
  }
}
